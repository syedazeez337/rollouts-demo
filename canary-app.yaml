apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rollout-canary
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/syedazeez337/rollouts-demo
    targetRevision: main
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
